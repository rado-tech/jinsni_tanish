# Hugging Face Spaces (Gradio SDK, CPU basic).
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchaudio            # resampling only; browser mics deliver 44.1/48 kHz
numpy>=1.26
gradio>=6.0,<7
